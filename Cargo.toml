# Its own workspace root, so this repository builds on its own.
[workspace]

[package]
name = "xmip-core-cluster"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
repository = "https://github.com/IlleNilsson/xmip-core-cluster"
description = "Xmip Cluster membership and capability-aware node selection."

[dependencies]
xmip-core = { git = "https://github.com/IlleNilsson/xmip-core", branch = "main" }
xmip-node = { package = "xmip-core-node", git = "https://github.com/IlleNilsson/xmip-core-node", branch = "main" }